<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.UserMapper">

    <resultMap id="userResult" type="bean.User">

        <result property="id" column="user_id"/>
        <result property="name" column="user_name"/>
        <result property="email" column="user_email"/>
        <result property="password" column="user_password"/>

        <collection property="blogs" ofType="bean.Blog" select="findAllBlogsByUserId" column="user_id">

        </collection>

    </resultMap>

    <select id="findById" parameterType="int" resultMap="userResult" resultType="bean.User">
        SELECT
        user.id AS user_id,
        user.name AS user_name,
        user.email AS user_email,
        user.password AS user_password
        FROM user WHERE user.id = #{id};
    </select>

    <select id="findAllBlogsByUserId"  parameterType="int" resultType="bean.Blog">
        SELECT * FROM blog WHERE blog.owner_id=#{user_id};
    </select>

</mapper>